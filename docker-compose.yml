version: '3.4'

services:
  infratokenarchitecture:
    image: infratoken-service
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
